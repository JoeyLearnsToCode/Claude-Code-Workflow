# Development Guidelines

## Overview

Use Chinese by default, for comments, git submission messages, and conversations.
When a bash command fails to execute, attempt to run the equivalent pwsh command instead.